# Конфиг ESLint для js проектов

## Установка

```bash
npm install eslint @massproject/eslint-config -D
```

## Использование
В файле `.eslintrc.js` прописать:
```js
module.exports = {
  extends: '@massproject/eslint-config',
};
```
